# سجل التغييرات — Starlink Diagnostic Pro

## v2.3.0 — فحص الهاردوير + استخراج الأخطاء + أخطاء GPS

### فحص العتاد الشامل (HARDWARE)
- **عملية `hardware_check` جديدة**: تقرير عتاد كامل في نداء واحد يبني كل ما يعلنه الطبق فعلياً عبر gRPC — دون أي اختراع.
- **بطاقة هوية الجهاز الكاملة**: رقم الطبق، إصدار العتاد والبرامج، Build، مراجعة اللوحة (board_rev)، إصدار التصنيع، حماية الرجوع (anti-rollback)، الجيل، تساوي الأقسام، عدد الإقلاعات، زمن التشغيل.
- **جاهزية الأنظمة الفرعية الست (DishReadyStates=1019)**: CADY (محرك التوجيه) · SCP (معالج التحكم) · L1L2 (طبقة الراديو) · XPHY (ناقل البيانات) · AAP (مصفوفة الهوائيات) · RF — كل وحدة بحالة جاهز/غير جاهز/غير معلن بلونها.
- **أدلة إعادة التشغيل (RebootReason=1032)**: 18 سبباً مترجماً بالعربية — «قطع طاقة حراري»، «موت عملية حرجة»، «انهيار AAP»، «نواة متأثرة»… وهي بصمات الأعطال المتقطعة التي «تعيد التشغيل ليلًا وحدها».
- **الحركة**: حالة المحركات (ACT_FAULTED تظهر كعطل)، زاوية الميل، حالة مرشح الاتجاه (IMU) وعدم اليقين.
- الحرارة والطاقة وسحب الطبق/الراوتر ضمن التقرير نفسه، مع حكم إجمالي (سليم / تنبيهات / أعطال) وتخزين النتيجة في قاعدة البيانات.

### استخراج الأخطاء (سجل موحد)
- **شاشة «الأخطاء والتنبيهات» جديدة** + عملية `errors_log`: تجمع كل عطل يعلنه الطبق من كل المصادر في قائمة واحدة مرتبة حسب الشدة (عطل → تحذير → معلومة).
- **جدول DishAlerts الكامل (20 تنبيهاً)** مترجماً بالعربية مع الشدة: محركات عالقة، إيقاف حراري، ماء داخل الطبق، تيار محركات منخفض، إشارة أدنى من المتوقعة، تيليمتري DBF متوقف، إيثرنت 100Mbps، منفذ UPSU بطيء… إلخ.
- مصادر التجميع: تنبيهات النظام + أكواد العتاد (الأجهزة الأقدم) + رمز الإيقاف (1024) + فشل التحديث (SWU_FAULTED) + المحركات والمرشح + سبب إعادة التشغيل + الانقطاع الجاري + آخر 5 انقطاعات من سجل الطبق (1009) + أخطاء GPS.
- شارة «أخطاء معلنة: N» في اللوحة الرئيسية تفتح السجل، وأزرار التحديث، وتخزين التنبيهات في قاعدة البيانات.

### أخطاء GPS (سلسلة مفصلة)
- **سجل أخطاء GPS في شاشة GPS**: كل خطأ بنداً مستقلاً غير متكافئ مع تفسير عربي واقتراح إجراء:
  - `GPS_HARDWARE_TEST_FAILURE` — فشل اختبار عتاد GPS (كود معلن)
  - `NO_SATS_AFTER_TTFF` — لا أقمار حتى بعد زمن أول إصلاح (مؤشر عطل هوائي GPS)
  - `GPS_INHIBITED` — موقوف عمداً (ليس عطلاً — مع توجيه لتفعيله من التحكم)
  - `NO_FIX` — لا إصلاح حالياً (غالباً مؤقت)
  - `PNT_FILTER_FAULTED/INVALID` — مرشح الملاحة والتوقيت فاشل/غير صالح (عطل صلب)
  - `PNT_FILTER_UNCONVERGED` — المرشح لم يستقر بعد (طبيعي بعد الإقلاع)
- قراءة الحقول الحقيقية: `no_sats_after_ttff` (gps_stats=3) و`pnt_filter_convergence_state` (gps_stats=5) إلى جانب valid/sats/inhibit.
- قاعدة التفريق: لا يُستنتج «لا إصلاح» عندما يفسر الـ inhibit الغياب، ولا يُستنتج عطل عتاد إلا بإعلان صريح من الطبق.

### إضافات
- بطاقة التنبيهات النشطة في Dashboard/Live عبر `activeAlerts` (المفاتيح الحقيقية للحقول بدل أسماء مركبة).
- تقرير PDF موسّع بأقسام: Active Alerts (DishAlerts) · Subsystems NOT Ready · GPS Error Ledger.
- خريطة المكونات الثمانية وأكواد العتاد القديمة بقيت في شاشة HARDWARE (متوافقة مع الأجهزة الأقدم).
- طبقة Python مختبرة بالكامل: قسم V2.3 جديد في `test_v2_bridge.py` (~40 فحصاً: hardware_check/errors_log/activeAlerts/readyStatesAr/reboot forensics/GPS issues E2E عبر mock الطبق) — ALL CHECKS PASSED.
- الإصدار: versionCode 5.

## v2.2.0 — الإصدار الاحترافي (الدقة + الشمول)

### الدقة
- **إحصاءات شبكية دقيقة في المراقبة المباشرة**: p50/p95/p99 للكمون + Jitter + متوسط التنزيل/الرفع + فقد الحزم، عبر نافذة 300 عينة (~5 دقائق) تُحسب من مخزن الطبق الحلقي مباشرة دون أي نداء gRPC إضافي.
- أوزان الكمون تُحسب على **العينات المتصلة فقط** — انقطاع كامل داخل النافذة لا يُفسد المئينيات (`nLat` تعرض عدد عينات الكمون المتاحة من إجمالي النافذة).
- **مؤشر جودة تدفق البيانات (Freshness)**: رصد تجمّد عدّاد `end_counter` لمدة ≥ 15 ثانية — إنذار مبكر بأن الطبق متصل لكنه توقف عن بث العينات، يظهر في المراقبة المباشرة قبل انقطاع الاتصال الكامل.

### الشمول
- **اتجاهات طويلة المدى** في شاشة السجل: توفر الخدمة %، عدد الانقطاعات (جولات فقد ≥ 90%)، كمون p50/p95، متوسط السرعات، واتجاه الكمون/التنزيل (تحسن / مستقر / تراجع بمقارنة نصفي النافذة بعتبة 10%) عبر نوافذ 6 ساعات / 24 ساعة / 7 أيام.
- **تصدير CSV**: تصدير السجل المحلي (1 ساعة – 7 أيام) إلى `starlink_history_<timestamp>.csv` مع مشاركته عبر قائمة النظام؛ الأعمدة: timestamp, datetime_iso, download_mbps, upload_mbps, latency_ms, packet_loss_pct, gps, obstruction_pct, state.
- **تقرير PDF موسّع**: قسم «Network Quality (window)» بالأوزان الجديدة + قسم «V42 Evidence» (انقطاع جارٍ وسببه، رمز الإيقاف، حالة التحديث، تقييد النطاق، محاذاة boresight الفعلية مقابل المطلوبة، سحب قدرة الطبق/الراوتر).

### الاحترافية
- **معالج أول تشغيل**: 3 خطوات (الاتصال بشبكة الطبق → جاهزية الطبق → أول تحديث حالة) يظهر مرة واحدة ويمكن إعادته من الإعدادات.
- **صحة اتصال ذكية**: شريط حالة ملون في اللوحة الرئيسية (متصل / جارٍ التحقق / تعذر الوصول مع عداد المحاولات) + تراجع تصاعدي في حلقة الاستقصاء (الفاصل ×2/×4/×8 بحد أقصى 120 ثانية عند الفشل المتكرر).
- **شاشة «حول التطبيق»**: خريطة البروتوكول (RPCs)، سجل الإصدارات الكامل، والمرجع المجتمعي.
- الإصدار: versionCode 4.

## v2.1.0 — محاذاة البروتوكول مع أجهزة 2026 (API v42)
- إعادة بناء مخطط gRPC على تخطيط v42 الحديث: إسناد أسباب الانقطاع من سجل الطبق (History.outages=1009)، الطاقة (power_in=1010 + upsu_stats=1043)، عدادات المحاذاة (alignment_stats=1027)، GPS الحقيقي (inhibit_gps)، أعلام SNR، رمز الإيقاف (1024)، حالة التحديث (1021)، تقييد النطاق (1044/1045).
- خريطة عرقلة قطبية (RPC 2008) بعرض Canvas قطبي.
- اختبار سرعة من الطبق نحو الـ POP (1027/1028)، تشخيص الراوتر وأجهزته (3004/3002)، أهداف ping الطبقية (1009).
- أوامر جديدة: تفعيل GPS (2014)، توفير طاقة/نوم (2013).

## v2.0.0 — الأساس
- محرك تشخيص بسلسلة أدلة (Self-Test → GPS chain → RF/PHY → Final).
- مراقبة مباشرة 1/5/10/30/60 ثانية، قاعدة `StarlinkDiagnostic.db`، GPS ثلاثي الحالات، RAW gRPC + JSON، تشخيص شبكة قفزة-بقفزة، أوامر Restart/Stow/Unstow، تقرير PDF.
- CI/CD: بناء APK موقّع arm64-v8a تلقائياً ونشره في GitHub Releases.
