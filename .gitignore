# Gradle
.gradle/
build/
app/build/
local.properties

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc

# Logs
*.log

# NOTE: keystore/ is intentionally NOT ignored — the release signing key
# is committed so that CI can produce installable signed APKs. Replace it
# with GitHub secrets if this repository ever becomes public-sensitive.
